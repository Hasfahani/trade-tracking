# Summary: Configures pytest for this project.
# Details: It stores pytest settings so the test suite uses the same default options each time.
[pytest]
addopts = -p no:cacheprovider
testpaths = tests
pythonpath = .
